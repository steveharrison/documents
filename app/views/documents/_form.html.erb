<%= form_for @document, :html => { :multipart => true } do |f| %>
  <% if @document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@document.errors.count, "error") %> prohibited this document from being saved:</h2>

      <ul>
      <% @document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  	<input class="button" id="document_attachment" name="attachment" type="file"/>

	<div class="field">
		<%= f.text_field :title, :placeholder => "Title" %>
	</div>
	
<!--	<div class="field">
		<%= f.label :content %><br/>
		<%= f.text_area :content %>
	</div>-->
	
	<script>
		$(document).ready(function() {
			$("#tag-field").tag({ height: 80 });
		});
	</script>
	
	<div id="tag-field"></div>
	
<!--	<div class="tag-box">
		<div class="tags">
			<div class="tag-tag">Tag Name</div>
			<div class="tag-tag">These</div>
			<div class="tag-tag">Beautiful?</div>
		</div>
		<input type="text" class="tag-input" placeholder="Add a tag..."/>
	</div>-->
	
	

	

  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
